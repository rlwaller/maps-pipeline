services:
  maps:
    environment:
      - PYTHONPATH=maps-ror
      - MPLBACKEND=Agg
    platform: linux/amd64
    build:
      context: .
      dockerfile: Dockerfile
    working_dir: /workspace
    volumes:
      - .:/workspace
    command: make test
